package fr.dogstellar.view;

import java.awt.ItemSelectable;
import java.awt.event.*;

import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.JPopupMenu;
import javax.swing.plaf.basic.BasicSliderUI.ActionScroller;

import com.sun.glass.ui.Menu;
import com.sun.java.swing.plaf.motif.MotifMenuUI;

import fr.dogstellar.core.Stuff;

/*public class mouseManagement extends MouseAdapter {
	private Stuff i;
	@Override

	
	// TODO Auto-generated method stub
	

	super.mouseMoved(e){
		//for (Stuff i : stuffs) {
			//if (i != thePlayer.armor && i != thePlayer.weapon) {
		JPopupMenu theMenu=new JPopupMenu("Item Properties");
		JPanel menu = new JPanel();
		theMenu.add(menu);
		JLabel nameItem=JLabel newJlabel(i.getInformation().getName());
		int features = 0;
		if (i isWeapon()) {
			 features = i.getDamage();
		}
		if (i.isPotion()) {
			 features = i.getPdv();
		}
		if (i.isArmor()) {
			 features = i.getArmor();
			
		}
		 JLabel feat =newJlabel (features); 
	};
		JLabel desc = new JLabel(i.getInformation().getDescritpion());
		menu.add (nameItem);
		menu.add (features);
		menu.add(desc);
}*/
